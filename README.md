# FinanceFinder

## Takes in a Chase/Amex credit card statement and allows the user to specify a category and preferred name for each item in the statement.
## This program will then automatically show the user each item with its associated category and sum each category for the user.